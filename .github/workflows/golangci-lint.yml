name: Golangci-lint Linter 检查
on:
  push:
  pull_request:
jobs:
  tests:
    runs-on: ubuntu-latest
    env:
      GO111MODULE: on
    steps:
      - name: Checkout Source
        uses: actions/checkout@v3
      - name: Golangci-lint
        uses: golangci/golangci-lint-action@v8.0.0
