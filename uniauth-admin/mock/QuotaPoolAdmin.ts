// @ts-ignore
import { Request, Response } from "express";

export default {
  "POST /quotaPool/admin/batchModify": (req: Request, res: Response) => {
    res.status(200).send({ ok: true, err: "2eWRZmx" });
  },
  "POST /quotaPool/admin/resetBalance": (req: Request, res: Response) => {
    res.status(200).send({ ok: false, err: "c3l[" });
  },
};
