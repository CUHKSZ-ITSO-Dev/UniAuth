# 生产环境 Dockerfile (用于 GitHub Actions)
FROM nginx:alpine

# 复制自定义 nginx 配置
COPY deploy/nginx.conf /etc/nginx/nginx.conf

# 从本地构建产物复制到 nginx
COPY --chown=nginx:nginx dist /usr/share/nginx/html

# 暴露端口
EXPOSE 80

# 启动 nginx
CMD ["nginx", "-g", "daemon off;"]
