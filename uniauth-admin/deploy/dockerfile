# 生产阶段 - 使用预构建的静态资源
FROM nginx:alpine

# 复制自定义 nginx 配置
COPY deploy/nginx.conf /etc/nginx/nginx.conf

# 从预构建的 dist 目录复制静态文件到 nginx
COPY --chown=nginx:nginx dist /usr/share/nginx/html

# 暴露端口
EXPOSE 80

# 启动 nginx
CMD ["nginx", "-g", "daemon off;"]
